{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": ["esnext", "webworker"],
    "types": ["audioworklet"]
  },
  // gives stronger, AudioContext typing in .worklet.ts files
  "include": ["src/**/*.worklet.ts"],
  "exclude": []
}
