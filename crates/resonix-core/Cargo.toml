[package]
name = "resonix-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[features]
js = ["dep:wasm-bindgen"]
test-utils = []

[dependencies]
thiserror-no-std = { workspace = true }
nohash-hasher = "0.2.0"
bon = "3.9.1"
wasm-bindgen = { workspace = true, optional = true }
