[package]
name = "resonix-cli-tests"
version = "0.1.0"
edition = "2024"

[dependencies]
resonix-audio = { path = "../resonix-audio/", features = [
	"cpal",
	"mock",
	"ringbuf",
] }
dasp_sample = "0.11"
