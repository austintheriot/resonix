[package]
name = "wasm-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true

[dependencies]
resonix-core = { path = "../../crates/resonix-core/" }
resonix-wasm-audio-node = { path = "../../crates/resonix-wasm-audio-node/" }
